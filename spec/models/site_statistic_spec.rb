require 'spec_helper'

describe SiteStatistic do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    SiteStatistic.create!(@valid_attributes)
  end
end
