<%= render 'shared/tweets'  if profile_artist? %>
<%= render profile_artist? ? 'shared/sidebar_followers' : 'shared/user_follows' %>
<%# render 'shared/user_top_mixes' %>
<%= render 'shared/top_stations_box'     unless profile_artist? %>

<%= render 'shared/similar_artists'  if profile_artist? %>

<% if params[:controller] == 'accounts' and params[:action] == 'show' %>
  <%= render 'shared/user_recent_activity'  %>
<% end %>

