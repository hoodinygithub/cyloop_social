jQuery(document).ready(function($){ 
	$('#recommended_items').html("<%= escape_javascript(render(:partial => 'shared/abstract_station', :collection => @recommended_stations, :locals => { :columns => 6 })) %>");
	setTimeout(function(){
		$('#recommended_stations_loader').fadeOut('slow', function(){			
			$('#recommended_items').fadeIn('slow');
		});		
	}, 1000);
});