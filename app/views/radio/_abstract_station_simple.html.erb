<% unless abstract_station_simple.nil? %>
  <% unless abstract_station_simple.artist.nil? %>
    <% artist = abstract_station_simple.artist %>
    <div class="station">
      <div class="avatar">
        <% if artist.nil? %>
          <%= avatar_for(nil, :artist_icon) %>
        <% else %>
          <% if @station_obj.nil? %>
          <%= link_to avatar_for(artist, :artist_icon), radio_path(:station_id => abstract_station_simple.station.id) %>
          <% else %>
          <%= link_to_function avatar_for(artist, :artist_icon), "Base.radio.set_station_details(#{abstract_station_simple.station.id}, #{abstract_station_simple.station_queue})" %>
          <% end %>
        <% end %>
      </div>
      <div class="station_info">
        <h2>
          <% if @station_obj.nil? %>
          <%= link_to abstract_station_simple.name, radio_path(:station_id => abstract_station_simple.station.id) %>
          <% else %>
          <%= link_to_function abstract_station_simple.name, "Base.radio.set_station_details(#{abstract_station_simple.station.id}, '#{abstract_station_simple.station_queue}')" %>
          <% end %>
        </h2>
        <%= station_contains(abstract_station_simple) %>
      </div>
    </div>
    <br class="clearer" />
  <% end %>
<% end %>
