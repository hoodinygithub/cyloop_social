<%= render 'shared/ajax_modules' %>
<% content_for :title do %>
  ACTIVITY
<% end %>

<% content_for :sorting do %>
  <%= link_to("#{profile_account.name} + FOLLOWING", '') %> /
  <%= link_to("#{profile_account.name}", '', :class => '') %> /
  <%= link_to("JUST FOLLOWING", '', :class => 'active') %>
<% end %>

<%= render 'shared/internal_content' %>

