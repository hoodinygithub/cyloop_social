- session[:display_layer] = nil
:javascript
  $(document).ready(function(){
    jQuery.facebox.settings.opacity = 0.35;
    jQuery(document).trigger('init.facebox')
    jQuery(document).trigger('loading.facebox')
    jQuery.facebox({ ajax: "#{session[:layer_to]}"})
  });

