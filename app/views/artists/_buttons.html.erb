<div class="big_black_box">
  <%= link_to(image_tag('play_button.png', :title => t('actions.play')), radio_path(:station_id => profile_artist.station.id), :class => 'play_button', :style => "float:left;" ) %>
  <div class="play_left_label" >
    <b>
      <span class="blue">
        <%= link_to profile_artist.station.name, radio_path(:station_id => profile_artist.station.id) %>
      </span>
    </b>
    <br/>
  </div>
  <br class="clearer" />
  <%= station_contains(profile_artist.station) %>
</div>
